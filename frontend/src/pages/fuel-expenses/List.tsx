// OWNER: Person C. Two sub-sections or tabs: Fuel Logs, Expenses.
// GET/POST /api/fuel-logs and GET/POST /api/expenses.

export default function FuelExpensesList() {
  return (
    <div className="p-6">
      <h1 className="text-lg font-semibold text-gray-900 mb-4">Fuel & Expenses</h1>
      <p className="text-sm text-gray-500">TODO: fuel log form + expense form + lists.</p>
    </div>
  );
}
